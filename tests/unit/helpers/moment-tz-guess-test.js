import { momentTzGuess } from 'embermeetup/helpers/moment-tz-guess';
import { module, test } from 'qunit';

module('Unit | Helper | moment tz guess');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentTzGuess([42]);
  assert.ok(result);
});
