import { moment } from 'embermeetup/helpers/moment';
import { module, test } from 'qunit';

module('Unit | Helper | moment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moment([42]);
  assert.ok(result);
});
