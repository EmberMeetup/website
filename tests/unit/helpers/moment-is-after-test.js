import { momentIsAfter } from 'embermeetup/helpers/moment-is-after';
import { module, test } from 'qunit';

module('Unit | Helper | moment is after');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentIsAfter([42]);
  assert.ok(result);
});
