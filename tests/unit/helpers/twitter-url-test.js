import { twitterUrl } from 'embermeetup/helpers/twitter-url';
import { module, test } from 'qunit';

module('Unit | Helper | twitter url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = twitterUrl([42]);
  assert.ok(result);
});
