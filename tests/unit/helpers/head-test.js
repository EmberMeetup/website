import { head } from 'embermeetup/helpers/head';
import { module, test } from 'qunit';

module('Unit | Helper | head');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = head([42]);
  assert.ok(result);
});
