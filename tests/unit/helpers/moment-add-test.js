import { momentAdd } from 'embermeetup/helpers/moment-add';
import { module, test } from 'qunit';

module('Unit | Helper | moment add');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentAdd([42]);
  assert.ok(result);
});
