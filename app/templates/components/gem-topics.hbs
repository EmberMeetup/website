{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#each topics as |topic|}}
    {{gem-topics-topic
      image=topic.presenter.featured.url
      title=topic.title
      description=topic.excerpt
      by-line=topic.presenter.title
    }}  
  {{/each}}
{{/if}}
  
