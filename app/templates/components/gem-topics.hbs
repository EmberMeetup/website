{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#each topics as |topic|}}
    {{gem-topics-topic
      image=topic.presenter.avatar
      title=topic.presenter.title
      description=topic.excerpt
      by-line=topic.presenter.title
    }}  
  {{/each}}
{{/if}}
  
