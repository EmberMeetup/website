{{#if (or media.isMobile media.isTablet)}}
  {{episode-strip-title-frame is-expanded=isExpanded episode=episode}}
  {{#liquid-if isExpanded}}
    {{episode-strip-details-frame episode=episode}}
  {{/liquid-if}}
{{else}}
  {{episode-strip-title-frame episode=episode}}
  {{episode-strip-details-frame episode=episode}}
{{/if}}